# Credentials — never commit
.env
.env.*
!.env.example

# Metabase CLI scratch/working files
.scratch/
.mb/

# Local-only workflow history - never shared/mixed across teammates
logs/

# OS / editor cruft
.DS_Store
*.log

# Local Claude Code settings
.claude/settings.local.json

# History-log-enforcement hook state (ephemeral per-session counter)
.claude/.history-log-pending

internal_view.md